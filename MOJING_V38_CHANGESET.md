# 墨境 V38 全站高级文游层

## 本轮
- 新增 `css/platform-v8.css`：统一全站沉浸式舞台、半透明高级面板、金色细线、移动端底部导航。
- 新增 `js/platform-v8.js`：统一首页 / 世界 / 剧情 / 地图 / 人物 / 助手 / 设置导航，提供返回与首页入口；采用非破坏式 DOM 编排，不接管既有业务渲染。
- 更新 `js/data.js` 到 v38：自动加载 v8 CSS/JS。

## 设计目标
- 从普通后台式页面进一步转向高级互动视觉小说平台。
- 保持现代、古风、西幻、科幻等世界观都可使用的中性暗色舞台。
- 桌面端顶部操作 + 底部主导航；移动端压缩为轻量底栏。
- 所有页面保留原业务 DOM 与 API，降低重写过程中的功能回归风险。

## 检查说明
- 已检查加载顺序：v8 在 runtime-v7 之后加载。
- 已避免使用 replaceChildren / innerHTML 重写业务页面。
- GitHub Pages 实际浏览器运行仍需待部署缓存刷新后验证；当前不能将静态提交成功等同于线上运行测试通过。

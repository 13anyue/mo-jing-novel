# 墨境AI视觉小说引擎 v7 小白操作教程

> 你好呀！欢迎来到墨境的世界。这份教程会手把手带你从零开始，学会使用这款古风AI视觉小说引擎。不用担心你是第一次接触，我们会一步一步来，像朋友聊天一样轻松 😊

---

## 目录

1. [项目概述：墨境v7是什么](#一项目概述墨境v7是什么)
2. [文件结构说明](#二文件结构说明)
3. [首次使用步骤](#三首次使用步骤)
4. [核心功能详解](#四核心功能详解)
5. [进阶功能](#五进阶功能)
6. [常见问题](#六常见问题)
7. [自定义修改指南](#七自定义修改指南)
8. [Git部署教程](#八git部署教程)

---

## 一、项目概述：墨境v7是什么

墨境是一款**纯前端**（意思就是不需要后端服务器）的AI视觉小说引擎。你可以把它理解为"在手机或电脑上写古风小说的神器"——它帮你把文字变成可以互动对话的视觉小说，还能配上角色立绘、场景背景、背景音乐，让故事活起来！

### v7版本主要功能一览

| 功能模块 | 用途说明 |
|---------|---------|
| **API设置** | 连接AI大模型（OpenAI、DeepSeek、文心一言等），让AI角色"开口说话" |
| **主页** | 你的总控制台，查看数据统计、快速进入各个功能 |
| **人物志（NPC资料册）** | 创建和管理角色，支持21项古风属性、立绘、头像、背景故事 |
| **天下图（地图）** | 用画布自由绘制故事世界地图，创建场景节点和路径连线 |
| **背景库** | 上传场景背景图，支持分类+子分类管理 |
| **音乐** | 上传BGM背景音乐，运行时一键切换 |
| **状态栏** | 显示角色属性、好感度、自定义数值条 |
| **提示词工坊** | 自定义AI的"思维模板"，让角色回复更贴合设定 |
| **仿向量记忆** | 智能记忆系统，自动"想起"对话中的相关内容 |
| **预设管理** | 保存你的完整配置，随时切换不同故事世界 |
| **正则引擎** | 自动检测文本并触发事件（比如检测到关键词就换场景） |

### 视觉风格

墨境的默认主题是**古风墨境风格**：
- 暖羊皮纸底色 `#F5E6D3`
- 金色点缀 `#C9A227`
- 墨色文字 `#2C1810`
- 衬线字体（宋体风格），非常有古籍韵味

---

## 二、文件结构说明

解压后你会看到这样一个文件结构：

```
ai-visual-novel-v7/
├── index.html          ← 【主入口】双击打开这个文件就能用！
├── manifest.json       ← PWA配置文件（用于"安装到桌面"）
├── README.md           ← 开发文档（可以不看）
│
├── css/                ← 样式文件夹
│   ├── style.css       ← 主要样式（按钮、布局、卡片）
│   └── theme.css       ← 主题配色（5套主题都在这里）
│
├── js/                 ← 功能代码文件夹（46个模块）
│   ├── app-v4.js       ← 主应用控制器（页面导航总管家）
│   ├── event-bridge.js ← 联动中枢（让各模块互通消息）
│   ├── storage-v3.js   ← 数据存储（帮你自动保存到浏览器）
│   ├── data.js         ← 工具函数
│   │
│   ├── api.js          ← API设置（连接AI的"电话线"）
│   ├── runtime-v3.js   ← 游戏运行时（对话、打字机效果）
│   ├── npc-v3.js       ← 人物志（角色管理核心）
│   ├── background-v3.js← 背景库（场景图片管理）
│   ├── map-v3.js       ← 天下图（Canvas地图系统）
│   ├── music.js        ← 音乐（BGM播放器）
│   ├── statusbar.js    ← 状态栏（属性显示）
│   ├── prompts.js      ← 提示词工坊（AI模板）
│   ├── memory.js       ← 仿向量记忆（智能记忆检索）
│   ├── presets.js      ← 预设管理（配置保存/加载）
│   ├── regex.js        ← 正则引擎（自动触发事件）
│   │
│   └── ...（还有更多模块，后面会介绍）
│
└── assets/             ← 图标和图片资源
    ├── icon-192.png
    └── icon-512.png
```

> 💡 **小白提示**：你不用修改任何代码就能正常使用。如果你想做个性化调整，修改 `css/theme.css` 和 `js/app-v4.js` 就够了。

---

## 三、首次使用步骤

### 第一步：部署到 GitHub Pages（手机端和PC端都能访问）

这是最简单、最推荐的部署方式，而且**完全免费**！

#### 电脑端操作（推荐用电脑完成首次部署）

**1. 准备文件**
- 确保你有一整个 `ai-visual-novel-v7` 文件夹

**2. 创建 GitHub 仓库**
- 打开 [github.com](https://github.com)，注册/登录账号
- 点击右上角 `+` → `New repository`
- 仓库名称填 `my-visual-novel`（也可以取别的名字）
- 选择 `Public`（公开）
- 勾选 `Add a README file`
- 点击 `Create repository`

**3. 上传文件**
- 进入仓库页面，点击 `Add file` → `Upload files`
- 把 `ai-visual-novel-v7` 文件夹里的**所有文件和文件夹**拖进去
- 等上传完成后，拉到最下面，点击 `Commit changes`

**4. 开启 GitHub Pages**
- 点击仓库顶部的 `Settings`（设置）
- 左侧菜单找到 `Pages`
- `Source` 部分选择 `Deploy from a branch`
- `Branch` 选择 `main`，文件夹选 `/(root)`
- 点击 `Save`

**5. 访问你的页面**
- 等待约1分钟（GitHub需要时间部署）
- 刷新页面，你会看到绿色提示框，显示类似：
  ```
  Your site is live at https://你的用户名.github.io/my-visual-novel/
  ```
- 点击这个链接，就能看到墨境的启动画面了！

#### 手机端访问

部署完成后，手机和电脑访问的是**同一个链接**：

```
https://你的用户名.github.io/my-visual-novel/
```

**添加到手机主屏幕（像App一样使用）：**

**iPhone（Safari）：**
1. 用 Safari 打开部署好的链接
2. 点击底部中间的「分享」按钮（方形带向上箭头的图标）
3. 选择「添加到主屏幕」→「添加」

**安卓手机（Chrome）：**
1. 用 Chrome 打开部署好的链接
2. 点击右上角「⋮」菜单
3. 选择「添加到主屏幕」或「安装应用」

> 📸 **【截图位置：GitHub Pages设置页面】**
> 这里建议截图标注：Settings → Pages → Branch选择main

---

### 第二步：设置API（让AI角色"开口说话"）

墨境支持所有 **OpenAI格式兼容** 的API，包括但不限于：

| 服务商 | 官网/说明 |
|-------|----------|
| OpenAI (GPT) | 官方API，需要国外信用卡 |
| DeepSeek | 国产大模型，性价比高 |
| 百度文心一言 | 千帆平台API |
| Claude | Anthropic官方 |
| 任意自定义接口 | 只要支持OpenAI格式的 `/chat/completions` 即可 |

**设置步骤：**

1. 打开墨境页面，点击左侧导航栏的 **「API设置」**

   > 📸 **【截图位置：侧边栏 → API设置按钮】**

2. 选择你的服务商：
   - 如果你用 **DeepSeek**：选择 "DeepSeek"，Base URL 会自动填入 `https://api.deepseek.com`
   - 如果你用 **自定义接口**：选择 "自定义接口"，手动填入 Base URL

3. 填入 **API Key**（这是你的"钥匙"，从服务商后台获取）：
   - DeepSeek：去 [platform.deepseek.com](https://platform.deepseek.com) 注册，充值后创建API Key
   - 其他服务商同理，一般在"开发者中心"或"API管理"页面

4. 选择**模型**：
   - DeepSeek 推荐：`deepseek-chat`（通用对话）
   - OpenAI 推荐：`gpt-4o-mini`（性价比高）

5. 点击 **「保存设置」**，然后点击 **「测试主API」**
   - 如果显示 "✓ 主API连接成功"，就设置好啦！
   - 如果失败，检查API Key是否正确、余额是否充足

> 💡 **小白提示**：API Key就像你的手机号密码，**不要分享给任何人**！墨境会把Key存在你自己的浏览器里，不会上传到任何服务器。

> 📸 **【截图位置：API设置页面，标注各个输入框】**

---

### 第三步：创建第一个角色

1. 点击左侧导航栏的 **「人物志」**
2. 点击右上角的 **「➕ 新建角色」** 按钮

   > 📸 **【截图位置：人物志页面 → 新建角色按钮】**

3. 填写角色信息（至少填**名字**，其他可以后面再补）：
   - **名字**：角色的称呼，比如"苏婉儿"
   - **性别**：女/男/其他
   - **年龄**：比如"18岁"
   - **职业**：比如"琴师"
   - **背景故事**：简单写一段，比如"自幼在江南长大，琴棋书画样样精通..."

4. 点击底部 **「保存」** 按钮

---

### 第四步：上传立绘和头像

**立绘**是角色在对话时显示的大图，**头像**是卡片视图里的小图。

**上传立绘：**
1. 在人物志页面，点击角色的卡片进入详情
2. 点击左侧立绘框，或者点击 **「修改」** 按钮
3. 在弹出的编辑器中，找到立绘上传区域
4. 点击上传区域，选择你准备好的角色图片（支持 JPG/PNG/WebP）
5. 等待上传完成，点击 **「保存」**

**关于立绘的小知识：**
- 立绘图推荐尺寸：竖版，高度大于宽度（比如 800x1200）
- 支持**透明背景**（PNG格式），这样角色可以"站在"场景背景上，效果更好
- 单张图片建议不超过 **2MB**，否则可能存储失败

> 📸 **【截图位置：角色编辑界面，标注立绘上传区域】**

---

### 第五步：上传背景音乐

1. 点击左侧导航栏的 **「音乐」**
2. 点击右上角的 **「➕ 上传」** 按钮
3. 在弹出的窗口中，点击上传区域
4. 选择你的音频文件（支持 MP3/WAV/OGG）
5. 填写名称，比如"长安街BGM"
6. 选择分类（背景音乐/音效/语音/环境音）
7. 选择是否循环播放
8. 点击 **「保存」**

**批量上传小技巧：**
- 点击上传窗口里的 **「批量」** 按钮
- 可以一次选择多首音乐文件
- 系统会自动批量导入

> 📸 **【截图位置：音乐上传窗口】**

---

### 第六步：开始一场对话

一切准备就绪！让我们来场沉浸式对话吧：

1. 点击左侧导航栏的 **「开始游戏」**
2. 在顶部选择角色、场景、音乐：
   - **角色**：下拉选择你刚才创建的"苏婉儿"
   - **场景**：下拉选择一个背景（如果还没上传背景，可以先选默认的）
   - **音乐**：选择刚才上传的BGM

   > 📸 **【截图位置：运行时页面顶部选择器】**

3. 在底部输入框中输入你想说的话，比如：
   ```
   苏姑娘，今天天气真好，不如我们去城外走走？
   ```

4. 点击发送按钮（或按 **回车键**）
5. 稍等片刻，AI会根据你的输入生成角色回复！

**运行时的小技巧：**
- 点击右上角的 **「设置」**（齿轮图标），可以调整：
  - 回复字数（简短/适中/长文）
  - 文风（正常/诗意/戏剧/简洁）
  - 是否开启打字机效果
  - 是否启用记忆、世界书等功能
- 点击 **「选项」** 按钮，AI会自动给你3个选择分支

---

## 四、核心功能详解

### 1. 人物志（NPC资料册）

人物志是整个墨境最核心的模块之一。你可以把它理解为"角色的身份证+档案袋"。

**如何创建角色：**

| 步骤 | 操作 |
|-----|------|
| 1 | 点击侧边栏「人物志」 |
| 2 | 点击「➕ 新建角色」 |
| 3 | 填写基础信息（名字必填） |
| 4 | 设置21项古风属性 |
| 5 | 上传立绘和头像 |
| 6 | 填写背景故事和对话风格 |
| 7 | 点击「保存」 |

**21项古风属性面板：**

这些属性决定了角色的"能力值"，数值范围是 **0-100**：

| 属性 | 说明 | 属性 | 说明 |
|-----|------|-----|------|
| 琴 | 音乐才艺 | 棋 | 棋艺谋略 |
| 书 | 书法文学 | 画 | 绘画艺术 |
| 歌 | 歌唱能力 | 舞 | 舞蹈技巧 |
| 骑 | 骑术 | 射 | 射箭武艺 |
| 绣 | 女红刺绣 | 厨 | 烹饪手艺 |
| 医 | 医术药理 | 孕 | 生育能力 |
| 统 | 统率领导 | 武 | 武力战斗 |
| 智 | 智慧谋略 | 政 | 政治手腕 |
| 魅 | 魅力气质 | 欲 | 欲望野心 |
| 经 | 经商理财 | 廉 | 清廉正直 |
| 思 | 思考深度 | | |

> 📸 **【截图位置：人物志详情页，标注属性面板区域】**

**标签页功能：**

人物志详情页有8个标签页：

| 标签 | 用途 |
|-----|------|
| 信息 | 基础档案 + 属性面板 + 喜好标签 + 自定义字段 |
| 生平记事 | 时间轴形式记录角色生平大事 |
| 日志 | 最近三天的日志会高亮显示 |
| 阴暗面 | 点击锁图标解锁，记录角色不为人知的秘密 |
| 秘密档案 | 加密存储，点击后才可见 |
| 秘密背包 | 角色私藏的物品清单 |
| 心情记录 | 用图表展示好感度变化趋势 |
| 关系图 | 查看该角色的人际关系网络 |

**自定义字段：**
- 点击「➕ 添加字段」，可以添加任意自定义属性
- 比如"师承"、"武功"、"门派"等
- 支持文本、数字、日期、多行文本、单选等类型

> 📸 **【截图位置：人物志标签页和自定义字段区域】**

---

### 2. 背景库

背景库用来管理故事中的场景图片。

**上传背景：**
1. 点击侧边栏「背景库」
2. 点击「➕ 上传背景」
3. 选择图片（支持单张、相册批量、URL批量三种模式）
4. 填写名称和分类
5. 点击「保存」

**分类管理：**
- 点击「📁 分类管理」按钮
- 可以创建新的分类（如"室内"、"室外"、"战斗场景"）
- 每个分类下还可以创建子分类
- 运行时只会从当前选中的分类中随机选取背景

> 📸 **【截图位置：背景库页面，标注分类栏和上传按钮】**

---

### 3. 音乐

音乐模块管理所有音频资源。

**支持的音频分类：**

| 分类 | 用途 |
|-----|------|
| 背景音乐 | 场景BGM，可循环播放 |
| 音效 | 按钮声、打击声等短音效 |
| 语音 | 角色配音（如果有） |
| 环境音 | 雨声、风声等氛围音 |

**运行时切换音乐：**
- 在「开始游戏」页面，顶部选择器可以直接切换BGM
- 音乐播放时，左上角播放器会显示当前曲目

---

### 4. 天下图（地图）

天下图是一个**Canvas 2D 无限地图**，你可以自由绘制故事世界的地理布局。

**基本操作：**

| 操作 | 效果 |
|-----|------|
| 滚轮 | 缩放地图 |
| 拖拽空白处 | 平移地图 |
| 右键空白处 | 弹出菜单（添加地点/设为当前位置） |
| 点击地点 | 选中该地点 |
| 拖拽地点 | 移动地点位置 |
| 点击地点A → 点击地点B | 建立两点之间的路径 |
| 右键路径 | 删除路径 |
| 双击地点 | 进入该地点的场景 |

**地点类型：**

| 类型 | 颜色 | 用途 |
|-----|------|------|
| 城镇 | 金色 | 城市、村落 |
| 野外 | 绿色 | 森林、草原 |
| 副本 | 红色 | 地下城、秘境 |
| 隐藏 | 灰色 | 秘密地点 |
| 商店 | 蓝色 | 商铺、市集 |
| 府邸 | 紫色 | 住宅、宫殿 |
| 寺庙 | 橙色 | 宗教建筑 |
| 水域 | 青色 | 河流、湖泊 |

**寻路功能：**
- 先在一个地点右键选择「设为当前位置」
- 点击另一个地点，选择「寻路至此」
- 系统会用 **Dijkstra算法** 找出最短路径，并用金色高亮显示

> 📸 **【截图位置：地图页面，标注各种交互操作】**

---

### 5. 状态栏

状态栏用来显示玩家或角色的实时状态。

**如何配置：**
1. 点击侧边栏「状态栏」
2. 在左侧配置区调整显示项：
   - **显示场景**：开关，是否在运行时显示当前场景名
   - **显示时间**：开关，是否显示游戏内时间
   - **显示世界**：开关，是否显示世界名

**添加自定义状态：**
1. 点击「➕ 状态项」按钮
2. 填写名称（如"体力"、"灵石"、"修为"）
3. 填写数值和最大值
4. 选择显示方式：
   - **进度条**：适合有最大值的属性（如体力 80/100）
   - **文本**：适合纯数值（如"灵石: 500"）

**添加NPC好感度：**
1. 点击「➕ 好感度」按钮
2. 选择要追踪的NPC
3. 设置当前值和最大值（如 50/100）
4. 保存后，运行时会在状态栏显示该NPC的好感度进度条

> 📸 **【截图位置：状态栏配置页面，左侧配置区+右侧预览区】**

---

### 6. 提示词系统

提示词是告诉AI"你是谁、该怎么说话"的指令。墨境内置了6类提示词模板。

**打开方式：**
- 点击侧边栏「提示词工坊」

**6类提示词：**

| 类型 | 用途 | 影响时机 |
|-----|------|---------|
| 系统提示词 | 告诉AI整体行为规则 | 每次对话都生效 |
| 角色模板 | 角色的详细设定 | 选择角色时注入 |
| 旁白 | 场景描述风格 | 需要旁白时 |
| 选项 | 生成选择分支的指令 | 点击"选项"按钮时 |
| 总结 | 对话总结的指令 | 自动保存记忆时 |
| 提取 | 提取关键记忆的指令 | 记忆入库时 |

**变量插值（魔法替换）：**

提示词中可以用 `{{变量名}}` 的语法，墨境会自动替换成实际内容：

| 变量 | 替换为 |
|-----|-------|
| `{{char_name}}` | 角色名字 |
| `{{char_personality}}` | 角色性格 |
| `{{char_background}}` | 角色背景 |
| `{{char_affection}}` | 好感度数值 |
| `{{scene}}` | 当前场景 |
| `{{player_name}}` | 玩家名字 |
| `{{memory}}` | 召回的记忆 |
| `{{history}}` | 最近对话历史 |

**修改提示词示例：**

假设你想让AI回复更简短：
1. 切换到「系统」标签页
2. 在系统提示词末尾加上：`每次回复控制在100字以内。`
3. 点击「💾 保存全部」

> 📸 **【截图位置：提示词工坊页面，标注标签页和变量列表】**

---

### 7. 仿向量记忆

这是墨境的"智能记忆"系统。它不是简单地记录对话，而是会在对话时**自动"想起"相关的往事**。

**两种工作模式：**

| 模式 | 原理 | 需要配置 |
|-----|------|---------|
| **TF-IDF** | 纯前端计算词频相似度 | 无需额外配置 |
| **Embedding** | 调用AI接口生成语义向量 | 需要在API设置中开启Embedding并配置API Key |

**如何添加记忆：**

1. 点击侧边栏「仿向量记忆」
2. 在「添加记忆」区域：
   - 输入记忆内容（如"苏婉儿小时候在江南长大"）
   - 选择分类（可选）
   - 填写场景（可选）
   - 添加标签（逗号分隔，如"童年,江南"）
3. 点击「添加」

**记忆检索：**
- 在「记忆检索」区域输入查询内容
- 选择分类筛选（可选）
- 设置返回条数（默认5条）
- 勾选「NPC隔离」可以让检索只返回当前角色的记忆
- 点击「检索」

**AI自动分类：**
- 输入记忆内容后，点击「AI分类」按钮
- AI会自动判断这条记忆属于哪个分类

> 📸 **【截图位置：仿向量记忆页面，标注添加区和检索区】**

---

### 8. 预设管理

预设是"配置的快照"——你可以把当前的API设置、提示词、状态栏、角色等打包保存，以后一键切换。

**保存预设：**
1. 点击侧边栏「预设管理」
2. 填写预设名称（如"古风修仙篇"）
3. 填写描述（可选）
4. 添加标签（可选，如"古风,修仙"）
5. 勾选要包含的配置项（默认全选）
6. 点击「保存」

**加载预设：**
- 在已保存预设列表中，点击「加载」
- 确认后，页面会自动刷新，所有配置就切换过来了

**运行时快速切换：**
- 在「开始游戏」页面，预设会显示为快捷按钮
- 点击即可快速切换，不影响当前对话历史

**分享预设：**
- 点击预设的「分享」按钮
- 会生成一段分享码，复制发给朋友
- 朋友在预设管理页面粘贴分享码即可导入

> 📸 **【截图位置：预设管理页面】**

---

### 9. 正则引擎

正则引擎是一个**自动文本处理工具**，可以检测对话内容并自动触发事件。

**基本用法：**

1. 点击侧边栏「正则引擎」
2. 在左侧规则列表中，点击「新建规则」
3. 填写规则信息：
   - **名称**：如"检测到'喜欢'就加好感"
   - **正则表达式**：如 `喜欢|爱|心动`（匹配包含这些词的内容）
   - **替换为**：留空表示删除匹配内容，或填入替换文本
   - **标志位**：通常填 `g`（全局匹配）
   - **应用场景**：选择标签（去重/去格式/提取/替换/清洗）

**八股文快捷处理：**
- 顶部工具栏下拉菜单提供常用处理：
  - 去除重复段落
  - 去除空行与多余空格
  - 去除特殊符号与乱码
  - 全角标点转半角 / 半角转全角

**规则链：**
- 左侧规则列表中，勾选启用的规则会加入规则链
- 点击底部「一键执行规则链」，按顺序批量处理文本

> 📸 **【截图位置：正则引擎页面，三栏布局】**

---

## 五、进阶功能

### 1. 表情差分（立绘多表情切换）

你可以为同一个角色准备多张不同表情的立绘，在对话中切换。

**设置方法：**
1. 在角色编辑界面，找到"表情差分"区域
2. 点击「添加表情」
3. 输入表情名称（如"微笑"、"生气"、"害羞"）
4. 上传对应的立绘图片
5. 保存

**运行时切换：**
- 在对话中，AI生成的内容如果包含情绪关键词
- 系统会自动匹配对应的表情差分
- 或者你可以手动在代码中调用表情切换

> 💡 **提示**：透明背景的PNG表情差分效果最好，可以和背景无缝融合。

---

### 2. 场景切换淡入淡出

当你更换背景图时，系统会自动播放淡入淡出动画：

1. 当前背景逐渐变暗（淡出）
2. 加载新背景图
3. 新背景逐渐变亮（淡入）

整个过程约 **0.6秒**，非常有电影感！

**触发方式：**
- 手动在运行时选择器切换背景
- 正则引擎检测到特定关键词自动切换
- 地图系统中双击地点进入场景

---

### 3. 触摸手势（移动端专属）

在手机上玩墨境时，支持以下手势操作：

| 手势 | 效果 |
|-----|------|
| 左滑 | 打开历史记录面板 |
| 上滑 | 快速存档 |
| 点击屏幕 | 跳过正在打字的内容，直接显示完整文本 |
| 双指缩放 | 调整立绘大小（如果界面支持） |

> 💡 **提示**：这些手势只在「开始游戏」页面的视觉小说模式下生效。

---

### 4. 主题切换

墨境内置了 **5套主题**，一键切换整个界面的视觉风格：

| 主题 | 风格 | 适合场景 |
|-----|------|---------|
| 古风墨境 | 暖羊皮纸+金色+墨色 | 古风、仙侠、历史 |
| 现代简约 | 白色+蓝色+灰色 | 都市、校园、现代 |
| 科幻未来 | 深蓝+青色+金色 | 科幻、未来、赛博朋克 |
| 校园清新 | 浅绿+白色+橙色 | 青春、校园、恋爱 |
| 暗黑深邃 | 黑色+紫色+青色 | 悬疑、恐怖、暗黑 |

**切换方法：**
- 通常在「设置」或「美化」页面中找到主题切换选项
- 点击即可实时生效，无需刷新页面

---

## 六、常见问题

### Q1：手机打不开页面怎么办？

**排查步骤：**

1. **检查网络**：确保手机联网（首次加载需要网络）
2. **换浏览器**：推荐 Chrome（安卓）或 Safari（iPhone）
3. **检查链接**：确认GitHub Pages链接拼写正确
4. **等待部署**：如果是刚上传的，等1-2分钟再试
5. **清理缓存**：浏览器设置 → 清除缓存后重试

> 💡 **提示**：GitHub Pages在国内访问可能较慢，可以多次刷新或更换网络环境。

---

### Q2：图片显示不出来（裂图）

**可能原因和解决方法：**

| 现象 | 原因 | 解决方法 |
|-----|------|---------|
| 立绘/背景不显示 | 图片太大超过2MB | 压缩图片后再上传 |
| 裂图图标 | 图片格式不支持 | 转换为 JPG 或 PNG |
| 之前能显示现在不行 | 浏览器缓存清理导致 | 重新上传图片 |
| URL背景不显示 | 跨域限制 | 使用自己的服务器或换成本地上传 |

**图片压缩工具推荐：**
- 电脑：[tinypng.com](https://tinypng.com)（在线压缩）
- 手机：应用商店搜索"图片压缩"

---

### Q3：API报错怎么处理？

**常见错误和解决方法：**

| 错误提示 | 原因 | 解决方法 |
|---------|------|---------|
| "HTTP 401" | API Key错误或过期 | 检查Key是否正确，重新生成 |
| "HTTP 429" | 请求太频繁 | 等几秒再试，或降低对话频率 |
| "HTTP 402" | 余额不足 | 去服务商后台充值 |
| "连接超时" | 网络问题 | 检查网络，或增加超时时间 |
| "CORS错误" | 跨域限制 | 确保API支持浏览器直接调用 |

**调试步骤：**
1. 打开 API设置 页面
2. 重新填入 API Key
3. 点击「测试主API」
4. 根据错误提示对照上表解决

> 💡 **DeepSeek用户特别注意**：确保你的账号有余额，新注册的账号通常有少量免费额度。

---

### Q4：数据丢失如何恢复？

**墨境的数据存储方式：**
- 所有数据存在浏览器的 `IndexedDB` 和 `localStorage` 中
- 这意味着：换浏览器、清理缓存、卸载App都会导致数据丢失

**预防措施（非常重要！）：**

1. **定期备份**：
   - 进入「备份管理」页面
   - 点击「创建备份」
   - 备份文件会下载为 `.json` 文件，保存好！

2. **导出预设**：
   - 在「预设管理」中导出你的配置

3. **PWA安装**：
   - 把墨境安装到主屏幕，可以减少误删缓存的概率

**数据恢复方法：**
1. 进入「备份管理」页面
2. 点击「恢复备份」
3. 选择之前下载的 `.json` 备份文件
4. 等待恢复完成

> ⚠️ **重要提醒**：请务必养成定期备份的习惯！推荐每次重要修改后备份一次。

---

## 七、自定义修改指南

### 1. 如何修改配色（theme.css）

打开 `css/theme.css`，找到你想要修改的主题。

**默认古风墨境主题的配色变量：**

```css
:root {
  --bg-body: #F5E6D3;        /* 页面背景色 */
  --bg-card: #FFF8F0;        /* 卡片背景色 */
  --text-primary: #2C1810;   /* 主要文字色 */
  --color-gold: #C9A227;     /* 金色强调色 */
  --color-primary: #8B4513;  /* 主色调 */
  --border-color: #D4C4A8;   /* 边框颜色 */
}
```

**修改示例**——把金色改成红色：

```css
/* 找到这行 */
--color-gold: #C9A227;

/* 改成 */
--color-gold: #C62828;
```

> 💡 **颜色代码工具**：不知道想要什么颜色？去 [colorhunt.co](https://colorhunt.co) 找配色灵感！

---

### 2. 如何修改字体

同样在 `css/theme.css` 中：

```css
--font-main: 'Noto Serif SC', 'Source Han Serif SC', 'SimSun', serif;
--font-display: 'Noto Serif SC', 'KaiTi', 'STKaiti', serif;
```

**修改示例**——换成更现代的字体：

```css
--font-main: 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', sans-serif;
--font-display: 'Noto Sans SC', sans-serif;
```

> 💡 **字体说明**：如果用户电脑/手机没有安装你指定的字体，会自动使用后面的备用字体。

---

### 3. 如何添加新的侧边栏菜单项

打开 `js/app-v4.js`，找到 `NAV_ITEMS` 数组。

**格式示例：**

```javascript
const NAV_ITEMS = [
  { id: 'home', label: '首页', icon: 'icon-home' },
  { id: 'runtime', label: '开始游戏', icon: 'icon-game' },
  { id: 'npc', label: '人物志', icon: 'icon-npc' },
  // ... 其他菜单项
  
  // 添加你的新菜单：
  { id: 'my-page', label: '我的页面', icon: 'icon-home' }
];
```

**然后添加页面处理：**

在 `callbacks` 对象中添加：

```javascript
const callbacks = {
  // ... 其他页面
  'my-page': () => { 
    // 这里写你的页面逻辑
    document.getElementById('page-my-page').innerHTML = 'Hello World';
  }
};
```

**最后在 index.html 中添加页面容器：**

```html
<section class="page-view" id="page-my-page"></section>
```

---

### 4. 每个JS文件对应什么功能

| 文件名 | 功能说明 | 修改场景 |
|-------|---------|---------|
| `app-v4.js` | 主应用控制器，管理页面导航 | 添加/删除页面、改菜单顺序 |
| `event-bridge.js` | 模块间通信中枢 | 让不同模块互相联动 |
| `storage-v3.js` | 数据持久化存储 | 修改存储策略 |
| `api.js` | API连接配置 | 添加新的AI服务商 |
| `runtime-v3.js` | 游戏运行时核心 | 修改对话UI、打字机效果 |
| `npc-v3.js` | 角色管理 | 添加新属性字段 |
| `background-v3.js` | 背景图管理 | 修改上传逻辑 |
| `map-v3.js` | 地图系统 | 修改地图样式 |
| `music.js` | 音乐播放器 | 添加音频格式支持 |
| `statusbar.js` | 状态栏 | 添加新的状态类型 |
| `prompts.js` | 提示词模板 | 修改默认提示词 |
| `memory.js` | 记忆系统 | 修改检索算法 |
| `presets.js` | 预设管理 | 添加新的配置项 |
| `regex.js` | 正则引擎 | 添加内置规则 |
| `theme.css` | 主题配色 | 修改颜色、字体、圆角 |
| `style.css` | 基础样式 | 修改布局、按钮样式 |

---

## 八、Git部署教程

### 用 Termux（安卓手机上的Linux终端）

Termux 是一款在安卓手机上运行Linux命令的App，非常适合手机端部署。

**安装 Termux：**
1. 在应用商店搜索"Termux"并安装
2. 打开 Termux，等待初始化完成

**基础 Git 命令清单：**

```bash
# 1. 安装 git
pkg install git

# 2. 配置你的身份信息（只需一次）
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"

# 3. 克隆你的仓库（把仓库下载到手机）
git clone https://github.com/你的用户名/my-visual-novel.git

# 4. 进入项目文件夹
cd my-visual-novel

# 5. 修改文件后，查看变更
git status

# 6. 添加所有修改的文件
git add .

# 7. 提交修改（写一句描述）
git commit -m "更新了角色立绘和背景"

# 8. 推送到 GitHub（会提示输入用户名和密码/Token）
git push

# 9. 如果 GitHub 上的文件有更新，先拉取最新版本
git pull
```

**使用 Personal Access Token 代替密码：**

GitHub 已不支持直接用密码推送，需要创建 Token：
1. 登录 GitHub，点击头像 → Settings
2. 左侧最下面 Developer settings → Personal access tokens → Tokens (classic)
3. 点击 Generate new token
4. 勾选 `repo` 权限
5. 生成后复制 Token，在 `git push` 时用这个 Token 代替密码

---

### 电脑端 Git 命令（和Termux基本一致）

```bash
# 初始化仓库（如果还没创建）
git init

# 添加远程仓库地址
git remote add origin https://github.com/你的用户名/my-visual-novel.git

# 日常更新流程
git add .
git commit -m "描述这次修改"
git push origin main
```

---

### 常用 Git 命令速查表

| 命令 | 作用 |
|-----|------|
| `git status` | 查看当前修改状态 |
| `git add .` | 添加所有修改的文件到暂存区 |
| `git add 文件名` | 添加指定文件 |
| `git commit -m "说明"` | 提交修改 |
| `git push` | 推送到远程仓库 |
| `git pull` | 从远程拉取最新代码 |
| `git log` | 查看提交历史 |
| `git diff` | 查看具体修改内容 |
| `git clone 链接` | 克隆远程仓库到本地 |
| `git branch` | 查看当前分支 |

---

### 部署到 GitHub Pages 后的更新

每次你用 `git push` 推送修改后：
1. GitHub 会自动重新部署（约1-2分钟）
2. 刷新你的页面 `https://你的用户名.github.io/my-visual-novel/`
3. 如果没看到更新，按 `Ctrl+F5`（电脑）或清除缓存后刷新（手机）

---

## 写在最后

恭喜你读完了这份教程！🎉

墨境是一个不断进化的项目，v7版本已经相当强大，但最好的作品永远来自你的创意。不要害怕尝试——所有的修改都可以备份恢复，所有的设置都可以重置。

如果在使用中遇到问题，记得：
1. 先检查这份教程的"常见问题"部分
2. 检查浏览器的控制台报错（按F12 → Console）
3. 在设置中导出备份，然后尝试重置

**祝你在墨境的世界里，写出最动人的故事！**

---

> 本教程适用于 墨境AI视觉小说引擎 v7
> 最后更新：2026-09-01


[DuMate AI生成]